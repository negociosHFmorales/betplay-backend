# render.yaml - Configuración específica para Render
services:
  - type: web
    name: betplay-backend
    runtime: python3
    buildCommand: |
      echo "🚀 Instalando dependencias para BetPlay Backend..."
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python app.py
    env: python3
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.4"
      - key: PORT
        value: "10000"
    scaling:
      minInstances: 1
      maxInstances: 1
